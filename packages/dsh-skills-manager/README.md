# @deepseek-ai/dsh-skills-manage

Host plugin for dsh-skills-manager. Registers the `skills-manage` settings namespace, discovers and imports SKILL.md libraries, and provides the dynamic SkillProvider backing switchable skill groups.
